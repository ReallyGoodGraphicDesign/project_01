export default function TextStyleSectionSubtitle({ children }) {
        return (
                <p className="max-w-2xl text-sm text-lime-300 sm:text-base uppercase font-bold">
                        {children}
                </p>
        )
}